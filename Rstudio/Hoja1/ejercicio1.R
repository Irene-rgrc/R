# Indicar para cada una de las siguientes ecuaciones si son o no lineales

# 3x1-4x2=24
X = c(3,-4)
b = c(24)

a = length(X)

for (X in 1:a) {
  if (is.integer(x[i])==FALSE){
    print("No es lineal")
  }
}

# x1 - x2 + 5x3 + (2^(1/2))x4 = 1

X = c(1,-1,5,(2^(1/2)))
b = 1

